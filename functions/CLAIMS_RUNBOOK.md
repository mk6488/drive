# DRIVE Claims Runbook

The trusted live claim apply runbook lives in `../docs/46-trusted-claims-live-apply-runbook.md`.

No live claim apply script exists yet. The functions workspace currently supports dry run review only.

Do not add service account files to this workspace. Do not call `setCustomUserClaims` without an explicit future approved step.
